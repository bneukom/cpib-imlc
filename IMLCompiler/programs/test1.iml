program test global 
 proc test(in copy a:int, in copy b:bool, x:int, z:int) do
  a:=1; 
  call test(1, true,3, 3); 
  call test2(true && 3 > 4 +-1) 
 endproc; 
 proc test2(a:bool) do 
  if a do skip else skip; skip endif
 endproc;
 var aa:int
do 
 skip 
endprogram