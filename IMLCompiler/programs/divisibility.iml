program divisibility() 
global 
	var l:[[int]];
	var max:int;
	var counter:int
do 
	debugin max init;
	
	l init := [];
	counter init := max;
	while counter > 0 do
		l := { x | x from max to 1 when x mod counter == 0} :: l;
		counter := counter - 1
	endwhile;
	
	debugout l
endprogram