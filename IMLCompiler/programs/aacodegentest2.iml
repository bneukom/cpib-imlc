program aacodegentest2() 
global 
 var l:[int];
 fun test(in copy p:int) returns var l2:[int]
 local
  var c:int;
  var l:[int]
 do
  l init := { x | x from 1 to -p when true };
  l2 init := l
 endfun
do 
 l init:= test(5);
 debugout l
endprogram