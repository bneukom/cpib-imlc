program aacodegentest3() 
global 
	fun sum(in copy const l:[int]) returns var r:int
	local 
	 	var x:int
	do
		if length l == 0 do
			r init := 0
		else
			x := 0;
			r init := head l + sum(tail l)
		endif
		
	endfun
do
	skip
endprogram